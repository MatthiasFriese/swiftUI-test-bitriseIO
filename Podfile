# Uncomment the next line to define a global platform for your project
platform :ios, '13.2'

target 'bitriseIO' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for bitrise io
  #pod 'Alamofire', '4.8.2'

  target 'bitriseIOTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
